name: Build and Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    name: Build for ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, ubuntu-latest]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install PyQt6 PyQt6-WebEngine pyinstaller

       # Windows 打包命令
      - name: Build with PyInstaller (Windows)
        if: matrix.os == 'windows-latest'
        run: |
          pyinstaller --noconsole --onefile --name "HeyGenControl_v1.0_Win" `
          --icon "heygen-logo.ico" `
          --add-data "styles.py;." `
          --add-data "browser.py;." `
          --add-data "ui_components.py;." `
          --add-data "heygen-logo.ico;." `
          --collect-all PyQt6.QtWebEngineCore `
          --collect-all PyQt6.QtWebEngineWidgets `
          main.py

      # Linux 打包命令
      - name: Build with PyInstaller (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: |
          pyinstaller --noconsole --onefile --name "HeyGenControl_v1.0_Linux" \
          --add-data "styles.py:." \
          --add-data "browser.py:." \
          --add-data "ui_components.py:." \
          --add-data "heygen-logo.ico:." \
          --collect-all PyQt6.QtWebEngineCore \
          --collect-all PyQt6.QtWebEngineWidgets \
          main.py
          # 给予可执行权限
          chmod +x dist/HeyGenControl_v1.0_Linux

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-artifacts-${{ matrix.os }}
          path: dist/

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: all-builds
          pattern: build-artifacts-*
          merge-multiple: true

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            all-builds/HeyGenControl_v1.0_Win.exe
            all-builds/HeyGenControl_v1.0_Linux
            HeyGenControl.desktop
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
